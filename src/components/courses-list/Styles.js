import styled from 'styled-components';

export const StyledCoursesList = styled.section`
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(270px, 1fr));
  grid-column-gap: 24px;
  grid-row-gap: 40px;
`;